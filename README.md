# Скетчбук — заметки с упором на рисование

Веб-приложение в духе OneNote, но с акцентом на рисование графическим планшетом
(поддержка давления пера, наклона через Pointer Events API — работает с Wacom и
аналогичными планшетами).

## Возможности

- Иерархия **Блокноты → Разделы → Страницы**, как в OneNote (создание,
  переименование, удаление через правый клик).
- Рисование пером с чувствительностью к давлению, маркер (полупрозрачный),
  ластик.
- Настройка цвета (палитра + произвольный цвет) и толщины линии.
- Undo / Redo (Ctrl+Z / Ctrl+Y), очистка страницы, экспорт страницы в PNG.
- Автосохранение в IndexedDB прямо в браузере — все заметки хранятся локально,
  бэкенд не нужен.
- Простая защита от касания ладонью: пока пишете пером, случайные касания
  экрана игнорируются короткое время.
- Фон страницы: чистый / линованный / клетка / точки (задаётся при создании
  страницы, легко расширить через UI).

## Запуск

```bash
npm install
npm run dev
```

Откройте адрес из терминала (обычно http://localhost:5173).

## Сборка

```bash
npm run build
npm run preview
```

## Структура проекта

- `src/types.ts` — модели данных (Notebook/Section/Page/Stroke).
- `src/db/` — слой IndexedDB (`db.ts`) и бизнес-логика CRUD (`repo.ts`).
- `src/draw/` — движок рисования: `CanvasController.ts` (Pointer Events,
  давление/наклон, undo/redo), `render.ts` (отрисовка штрихов переменной
  толщины), `background.ts` (фоновые паттерны страницы).
- `src/ui/` — `sidebar.ts` (дерево блокнотов) и `toolbar.ts` (панель
  инструментов рисования).
- `src/main.ts` — сборка приложения, автосохранение.

## Дальнейшие идеи (не реализовано)

- Текстовые заметки/блоки рядом с рисунками (сейчас страница — чистый холст).
- Синхронизация между устройствами (сейчас всё хранится только локально в
  браузере).
- Более умное распознавание руки на экране с тачем (сейчас — по таймингу).
- Лассо-выделение и перемещение штрихов, слои.
- Undo с точной историей (сейчас undo/redo работают по стеку штрихов).
