# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
# Symlink dist -> frontend, создаётся .devcontainer/setup.sh для Codespaces
frontend

# Локальные артефакты сборки/деплоя — не должны попадать в репозиторий
*.zip
notes-app-deploy/

# Сервер: сгенерированные при первом запуске файлы (база, секрет сессии,
# пароль первого админа) — специфичны для конкретного развёртывания
server/venv
server/users.db
server/.flask_secret_key
server/INITIAL_ADMIN_CREDENTIALS.txt

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
